---
// can't seem to find a way to reliably extract
// that from Astro.site or similar
const baseurl = 'intro'

interface Props {
    label: string
    file: string
}

const { label, file } = Astro.props

---

<a href=`/${baseurl}/${file}#${label}` class="labeled link">
    <slot />
</a>
